services:

  order-service:
    image: order-service
    container_name: order-service

    ports:
      - "8082:8082"

    networks:
      - shopsphere-network

networks:
  shopsphere-network:
    external: true